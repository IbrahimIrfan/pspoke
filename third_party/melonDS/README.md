# melonDS headers (GPL-3.0)

These four headers come from [melonDS](https://github.com/melonDS-emu/melonDS) (copyright the melonDS team, GPL-3.0 or later).
pspoke's 2D renderer (`port/native-render-opt/GPU2D_Soft.cpp`, `native_gpu.cpp`) is derived from melonDS's software 2D renderer
and includes them. Their original copyright/license notices are kept unchanged. See the top-level `LICENSE`.
